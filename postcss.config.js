// Configuração do PostCSS (Tailwind + Autoprefixer)
export default {
  plugins: {
    tailwindcss: {}, // Processa classes do Tailwind
    autoprefixer: {}, // Adiciona prefixos automáticos (compatibilidade entre navegadores)
  },
};
